<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
    Hardened-runtime entitlements for the MAIN Zano.app executable only.

    QtWebEngineProcess is deliberately NOT covered here. That helper ships its
    own entitlements inside the framework, at
      QtWebEngineCore.framework/Helpers/QtWebEngineProcess.app/Contents/Resources/QtWebEngineProcess.entitlements
    and utils/macos_codesign.sh signs it with those. Applying this file to the
    helper instead, which is what "codesign deep signing" does, leaves it
    without the JIT entitlements Chromium needs; it then crashes on launch
    under the hardened runtime, taking the whole UI with it. Do not go back to
    signing this bundle with the deep flag.
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.disable-executable-page-protection</key>
    <true/>
</dict>
</plist>
