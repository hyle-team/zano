import { InputValidateDirective } from './input-validate.directive';

describe('InputValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new InputValidateDirective();
    expect(directive).toBeTruthy();
  });
});
