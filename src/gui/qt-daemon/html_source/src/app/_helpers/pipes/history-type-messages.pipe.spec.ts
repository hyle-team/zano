import { HistoryTypeMessagesPipe } from './history-type-messages.pipe';

describe('HistoryTypeMessagesPipe', () => {
  it('create an instance', () => {
    const pipe = new HistoryTypeMessagesPipe();
    expect(pipe).toBeTruthy();
  });
});
