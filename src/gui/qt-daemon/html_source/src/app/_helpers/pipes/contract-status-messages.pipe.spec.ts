import { ContractStatusMessagesPipe } from './contract-status-messages.pipe';

describe('ContractStatusMessagesPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractStatusMessagesPipe();
    expect(pipe).toBeTruthy();
  });
});
