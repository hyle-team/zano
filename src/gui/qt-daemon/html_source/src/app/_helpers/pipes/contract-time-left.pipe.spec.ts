import { ContractTimeLeftPipe } from './contract-time-left.pipe';

describe('ContractTimeLeftPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractTimeLeftPipe();
    expect(pipe).toBeTruthy();
  });
});
